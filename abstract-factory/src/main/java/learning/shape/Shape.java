package learning.shape;

/**
 * The Shape interface represents a geometric shape that can be drawn.
 * Implementing classes should override the draw() method to provide the drawing functionality for a specific shape.
 */
public interface Shape {
    void draw();
}